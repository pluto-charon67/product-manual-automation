{
  "name": "product-manual-automation",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Core scripts and MCP server for the Product Manual Automation plugin.",
  "engines": {
    "node": ">=22.5"
  },
  "scripts": {
    "mcp": "node mcp-server/index.mjs",
    "init:demo": "node scripts/init-workspace.mjs --project ./test/fixtures/demo-project --platforms web",
    "test": "node --test test/*.test.mjs",
    "self-test": "node scripts/self-test.mjs"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.18.2",
    "mysql2": "^3.14.5",
    "pg": "^8.16.3",
    "sharp": "^0.35.4",
    "yaml": "^2.8.1",
    "zod": "^3.25.76"
  }
}
